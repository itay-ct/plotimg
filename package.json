{
  "name": "plotimg",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @plotimg/frontend --filter @plotimg/backend dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
